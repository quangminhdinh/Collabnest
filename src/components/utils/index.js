import checkDynamicId from './checkDynamicId';
// import withURLgathering from './withURLgathering';
import stringToColor, { randomColor } from './stringToColor';

export { checkDynamicId, stringToColor, randomColor};